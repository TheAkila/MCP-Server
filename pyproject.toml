[project]
name = "demo"
version = "0.1.0"
description = "Simple MCP servers (calculator + file service) with a LangChain client"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.2.0",
    "fastmcp>=2.14.5",
    "python-dotenv>=1.2.1",
    "langchain>=0.3.0",
    "langchain-openai>=0.2.0",
    "langchain-mcp-adapters>=0.1.0",
]
